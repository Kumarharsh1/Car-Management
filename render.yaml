services:
  - type: web
    name: car-management-api
    env: docker
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/parking-lot-api-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
      - key: JAVA_OPTS
        value: "-Xmx256m"
